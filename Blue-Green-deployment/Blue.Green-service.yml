apiVersion: v1
kind: Service
metadata:
  name: blue-green-svc
  namespace: bluegreen

spec:
  selector:
    app: nginx
    version: blue

  type: NodePort

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30011
    protocol: TCP
